# Gearbox Settings Assets

This crate owns Gearbox-specific settings and keymap assets.

Edit files under `assets/settings` and `assets/keymaps` when localizing Gearbox.
Do not edit the repository-level `assets/settings` or `assets/keymaps` for Gearbox-only text, because those files are shared by upstream Zed.
